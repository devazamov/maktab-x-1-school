const state={user:null,page:"home",dashboard:null,lang:"uz",activity:null,timer:null};

const icons={home:"⌂",diary:"▣",grades:"★",homework:"✓",coin:"◉",shop:"🛍",challenge:"🏆",leader:"🥇",events:"📅",ai:"✦",profile:"◉",settings:"⚙",admin:"▦"};

async function api(url,opts={}){const r=await fetch("/api"+url,{credentials:"include",...opts,headers:{"Content-Type":"application/json",...(opts.headers||{})}});let j={};try{j=await r.json()}catch{}if(!r.ok)throw new Error(j.error||"Xatolik");return j}
function toast(s){const e=document.createElement("div");e.className="toast";e.textContent=s;document.getElementById("toast-root").append(e);setTimeout(()=>e.remove(),2600)}
function money(n){return new Intl.NumberFormat("uz-UZ").format(n)}
function esc(s){return String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}

async function boot(){try{const m=await api("/auth/me");state.user=m.user;await loadDashboard();render()}catch{renderLogin()}}
async function loadDashboard(){state.dashboard=await api("/dashboard")}
function renderLogin(){
 document.getElementById("app").innerHTML=`<main class="login"><section class="login-card"><div class="login-art"><img src="/assets/characters/x-boy.svg"></div><div class="login-form"><div class="logo"><img src="/assets/logo.svg"><div>MAKTAB X<small>1 SCHOOL</small></div></div><h1>Xush kelibsiz! 👋</h1><p class="muted">Hisobingizga kiring va bilim sari yangi qadam qo‘ying.</p><form id="loginForm"><div class="field"><label>Email</label><input id="email" type="email" value="student@maktabx.local" required></div><div class="field"><label>Parol</label><input id="password" type="password" value="Student123!" required></div><button class="primary" style="width:100%">Kirish</button></form><div class="demo"><b>Demo:</b><br>Student: student@maktabx.local / Student123!<br>Teacher: teacher@maktabx.local / Teacher123!<br>Admin: admin@maktabx.local / Admin123!</div></div></section></main>`;
 document.getElementById("loginForm").onsubmit=async e=>{e.preventDefault();try{const j=await api("/auth/login",{method:"POST",body:JSON.stringify({email:email.value,password:password.value})});state.user=j.user;await loadDashboard();render();toast("Xush kelibsiz!")}catch(e){toast(e.message)}}
}
function layout(content){
 const role=state.user.role;
 const admin=role==="ADMIN"||role==="SUPER_ADMIN";
 const nav=[
  ["home","Bosh sahifa"],["diary","Kundalik"],["grades","Baholar"],["homework","Uy vazifalari"],["coin","X Coin"],["shop","CoinShop"],["challenge","Challenge"],["leader","Leaderboard"],["events","Tadbirlar"],["ai","AI Assistant"],["profile","Profil"]
 ];
 if(admin)nav.push(["admin","Admin panel"]);
 return `<div class="shell"><aside class="sidebar"><div class="brand"><div class="logo"><img src="/assets/logo.svg"><div>MAKTAB X<small>1 SCHOOL</small></div></div></div><nav class="nav">${nav.map(n=>`<button class="${state.page===n[0]?'active':''}" onclick="go('${n[0]}')">${icons[n[0]]} &nbsp;${n[1]}</button>`).join("")}</nav><div class="side-bottom"><button class="nav" style="width:100%;text-align:left;background:#f7f9fc;border:0;border-radius:16px;padding:12px" onclick="logout()">↪ Chiqish<br><small>${esc(state.user.email)}</small></button></div></aside><main class="main"><header class="topbar"><input class="search" placeholder="Qidirish..."><div class="top-actions"><span class="coin-pill">🪙 ${money(state.user.balance)} X Coin</span><span>🔔</span><div class="avatar">${esc(state.user.name[0])}</div></div></header><div class="content">${content}</div><div class="mobile-nav">${nav.slice(0,5).map(n=>`<button onclick="go('${n[0]}')"><b>${icons[n[0]]}</b>${n[1]}</button>`).join("")}</div></main></div>`;
}
function go(page){state.page=page;render()}
function render(){
 if(!state.user)return renderLogin();
 const p=state.page;
 if(p==="home")return renderHome();
 if(p==="shop")return renderShop();
 if(p==="grades"||p==="diary")return renderGrades();
 if(p==="challenge")return renderChallenges();
 if(p==="leader")return renderLeader();
 if(p==="events")return renderEvents();
 if(p==="ai")return renderAI();
 if(p==="homework")return renderHomework();
 if(p==="coin")return renderCoins();
 if(p==="admin")return renderAdmin();
 if(p==="profile")return renderProfile();
}
function setApp(c){document.getElementById("app").innerHTML=layout(c)}

function renderHome(){
 const d=state.dashboard; const avg=d.stats.average;
 setApp(`<div class="welcome"><div><h1>Salom, ${esc(state.user.name.split(" ")[0])}! 👋</h1><p class="muted">Bugun yangi bilimlar sari yana bir qadam.</p></div><button class="primary" onclick="go('shop')">CoinShop →</button></div>
 <section class="hero"><div><span class="badge green">MAKTAB X</span><h2 style="font-size:24px;margin-top:14px">Bilim ol, challenge bajar,<br>X Coin bilan mukofotlan!</h2><p class="muted">Faolligingiz va yutuqlaringiz shu yerda.</p><button class="primary" onclick="go('challenge')">Challenge ko‘rish</button></div><img src="/assets/characters/x-boy.svg"></section>
 <div class="grid4"><div class="card stat"><span class="label">X Coin balans</span><div class="value">🪙 ${money(d.stats.balance)}</div><span class="hint">+120 bu hafta</span></div><div class="card stat"><span class="label">O‘rtacha baho</span><div class="value">${avg}</div><span class="hint">Yaxshi 👍</span></div><div class="card stat"><span class="label">Davomat</span><div class="value">${d.stats.attendance}%</div><span class="hint">Zo‘r ✓</span></div><div class="card stat"><span class="label">Level</span><div class="value">${d.stats.level}</div><span class="hint">${d.stats.xp} XP</span></div></div>
 <div class="section-title"><h2>Tezkor bo‘limlar</h2></div><div class="quick">${[["diary","📅","Kundalik"],["grades","⭐","Baholar"],["homework","📚","Uy vazifalari"],["coin","🪙","X Coin"],["shop","🛍","CoinShop"],["challenge","🎯","Challenge"],["ai","🤖","AI Assistant"]].map(x=>`<button onclick="go('${x[0]}')"><span class="ico">${x[1]}</span><span>${x[2]}</span></button>`).join("")}</div>
 <div class="grid2"><div><div class="section-title"><h2>Bugungi vazifangiz</h2><span class="badge">3 ta vazifa</span></div><div class="card list">${d.challenges.map(c=>`<div class="list-item"><div class="rowflex"><div class="iconbox">🏆</div><div><b>${esc(c.title)}</b><div class="muted" style="font-size:12px">${esc(c.description)}</div></div></div><span class="badge gold">+${c.reward}</span></div>`).join("")}</div></div><div><div class="section-title"><h2>So‘nggi yutuqlar</h2></div><div class="card list">${["Top Reader","Perfect Week","Challenge Master"].map((x,i)=>`<div class="list-item"><div class="rowflex"><div class="iconbox">${["📚","🌟","🏆"][i]}</div><b>${x}</b></div><span class="badge green">Yutuq</span></div>`).join("")}</div></div></div>`);
}

async function renderShop(){
 let products=[];try{products=await api("/shop/products")}catch(e){return toast(e.message)}
 setApp(`<div class="welcome"><div><h1>CoinShop</h1><p class="muted">X Coinlaringizni foydali mukofotlarga almashtiring.</p></div><span class="coin-pill">🪙 ${money(state.user.balance)} X Coin</span></div><div class="section-title"><h2>Mahsulotlar</h2><span class="badge">${products.length} ta</span></div><div class="product-grid">${products.map(p=>`<div class="card product"><img src="${p.image||'/assets/shop/notebook.svg'}"><h3>${esc(p.name)}</h3><p class="muted" style="font-size:12px">${esc(p.description)}</p><div class="row"><span class="price">🪙 ${money(p.price)}</span><button class="primary" onclick="buy('${p.id}')">Sotib olish</button></div><small class="muted">Omborda: ${p.stock}</small></div>`).join("")}</div>`);
}
async function buy(id){try{await api("/shop/purchase",{method:"POST",body:JSON.stringify({productId:id})});await loadDashboard();state.user.balance=state.dashboard.user.balance;render();toast("Xarid muvaffaqiyatli!")}catch(e){toast(e.message)}}

async function renderGrades(){
 const rows=await api("/grades");setApp(`<div class="welcome"><div><h1>Baholar</h1><p class="muted">Barcha fanlar bo‘yicha 1–10 baholar.</p></div>${["TEACHER","ADMIN","SUPER_ADMIN"].includes(state.user.role)?'<button class="primary" onclick="gradeModal()">+ Baho qo‘shish</button>':""}</div><div class="card" style="margin-top:20px"><table><thead><tr><th>Sana</th><th>Fan</th><th>O‘quvchi</th><th>Baho</th><th>Izoh</th></tr></thead><tbody>${rows.map(g=>`<tr><td>${g.date}</td><td><b>${esc(g.subject)}</b></td><td>${esc(g.student)}</td><td><span class="badge ${g.grade>=9?'green':g.grade>=6?'gold':'red'}">${g.grade}</span></td><td>${esc(g.comment)}</td></tr>`).join("")}</tbody></table></div>`);
}
function gradeModal(){document.body.insertAdjacentHTML("beforeend",`<div class="modal-back" id="modal"><div class="modal"><div class="modal-head"><h2>Baho qo‘shish</h2><button class="ghost" onclick="modal.remove()">✕</button></div><div class="field"><label>O‘quvchi ID</label><input id="gs"></div><div class="field"><label>Fan ID</label><input id="gsub"></div><div class="field"><label>Baho (1–10)</label><input id="gg" type="number" min="1" max="10"></div><div class="field"><label>Izoh</label><input id="gc"></div><button class="primary" onclick="addGrade()">Saqlash</button></div></div>`)}
async function addGrade(){try{await api("/grades",{method:"POST",body:JSON.stringify({studentId:gs.value,subjectId:gsub.value,grade:gg.value,comment:gc.value})});modal.remove();render();toast("Baho saqlandi")}catch(e){toast(e.message)}}

async function renderChallenges(){const cs=await api("/challenges");setApp(`<div class="welcome"><div><h1>Challenge</h1><p class="muted">Har kuni o‘zingizni sinang va X Coin yuting.</p></div></div><div class="grid2">${cs.map(c=>`<div class="card"><div class="rowflex"><div class="iconbox">🏆</div><div><h3>${esc(c.title)}</h3><p class="muted">${esc(c.description)}</p></div></div><div style="margin:18px 0"><div class="progress"><i style="width:${(c.progress||3)/c.target*100}%"></i></div><small class="muted">${c.progress||3}/${c.target}</small></div><div class="row"><span class="price">+${c.reward} X Coin</span><button class="primary" onclick="progressChallenge('${c.id}')">Davom etish</button></div></div>`).join("")}</div>`)}
async function progressChallenge(id){try{await api(`/challenges/${id}/progress`,{method:"POST",body:JSON.stringify({amount:1})});await loadDashboard();state.user.balance=state.dashboard.user.balance;render();toast("Challenge progress yangilandi")}catch(e){toast(e.message)}}

async function renderLeader(){const rows=await api("/leaderboard");setApp(`<div class="welcome"><div><h1>Leaderboard 🥇</h1><p class="muted">X Coin bo‘yicha eng faol o‘quvchilar.</p></div></div><div class="card" style="margin-top:20px"><table><thead><tr><th>#</th><th>O‘quvchi</th><th>Sinf</th><th>X Coin</th></tr></thead><tbody>${rows.map((u,i)=>`<tr><td><b>${i+1}</b></td><td><div class="rowflex"><div class="avatar">${esc(u.name[0])}</div><b>${esc(u.name)}</b></div></td><td>10-A</td><td>🪙 ${money(u.balance)}</td></tr>`).join("")}</tbody></table></div>`)}

async function renderEvents(){const es=await api("/events");setApp(`<div class="welcome"><div><h1>Tadbirlar</h1><p class="muted">Maktab tadbirlari va QR orqali qatnashuv.</p></div></div><div class="grid3" style="margin-top:20px">${(es.length?es:[{title:"Bilimlar olimpiadasi",description:"Maktab ichki olimpiadasi",date:"2026-09-10",location:"Aktlar zali"}]).map(e=>`<div class="card"><div class="iconbox">📅</div><h3 style="margin-top:14px">${esc(e.title)}</h3><p class="muted">${esc(e.description||"")}</p><p>📍 ${esc(e.location||"Maktab")}</p><p>🗓 ${esc(e.date||"")}</p>${state.user.role==="STUDENT"?`<button class="primary" onclick="checkin('${e.id||""}')">QR orqali qatnashish</button>`:""}</div>`).join("")}</div>`)}
async function checkin(id){if(!id)return toast("Demo event: QR hali yaratilmagan");try{await api(`/events/${id}/checkin`,{method:"POST"});toast("Tadbirga qatnashuv qayd etildi")}catch(e){toast(e.message)}}

async function renderAI(){setApp(`<div class="welcome"><div><h1>MAKTAB X AI 🤖</h1><p class="muted">Darslarni tushunish, savollar va mashqlar uchun yordamchi.</p></div></div><div class="card" style="max-width:900px;margin-top:20px"><div id="chat" class="list" style="min-height:300px"><div class="list-item"><div class="rowflex"><div class="iconbox">✦</div><div><b>X AI</b><div class="muted">Salom! Qaysi mavzuni tushuntirib beray?</div></div></div></div></div><form id="aiform" style="display:flex;gap:10px;margin-top:16px"><input id="aimsg" class="field" style="margin:0;flex:1;padding:14px" placeholder="Savolingizni yozing..."><button class="primary">Yuborish</button></form></div>`);aiform.onsubmit=async e=>{e.preventDefault();const m=aimsg.value.trim();if(!m)return;chat.insertAdjacentHTML("beforeend",`<div class="list-item"><b>Siz:</b> ${esc(m)}</div>`);aimsg.value="";try{const j=await api("/ai/chat",{method:"POST",body:JSON.stringify({message:m})});chat.insertAdjacentHTML("beforeend",`<div class="list-item"><div class="rowflex"><div class="iconbox">✦</div><div><b>X AI</b><div>${esc(j.answer)}</div></div></div></div>`)}catch(e){toast(e.message)}}}

async function renderHomework(){const hs=await api("/homework");setApp(`<div class="welcome"><div><h1>Uy vazifalari</h1><p class="muted">Topshiriqlar, deadline va fikrlar.</p></div></div><div class="grid2" style="margin-top:20px">${(hs.length?hs:[{title:"Matematika: Kasrlar",description:"Kasrlarni takrorlash va 10 ta misol yechish",deadline:"Bugun"}]).map(h=>`<div class="card"><span class="badge gold">Topshiriq</span><h3 style="margin-top:12px">${esc(h.title)}</h3><p class="muted">${esc(h.description||"")}</p><p>⏰ ${esc(h.deadline||"")}</p><button class="secondary">Topshiriqni ko‘rish</button></div>`).join("")}</div>`)}

async function renderCoins(){const c=await api("/coins");setApp(`<div class="welcome"><div><h1>X Coin 🪙</h1><p class="muted">Topgan va sarflagan X Coinlaringiz tarixi.</p></div><span class="coin-pill">🪙 ${money(c.balance)}</span></div><div class="card" style="margin-top:20px"><table><thead><tr><th>Sana</th><th>Sabab</th><th>Turi</th><th>Miqdor</th></tr></thead><tbody>${c.transactions.map(x=>`<tr><td>${new Date(x.createdAt).toLocaleString("uz-UZ")}</td><td>${esc(x.reason)}</td><td>${esc(x.type)}</td><td class="${x.amount>0?'green-text':''}">${x.amount>0?"+":""}${money(x.amount)}</td></tr>`).join("")}</tbody></table></div>`)}

async function renderAdmin(){
 if(!["ADMIN","SUPER_ADMIN"].includes(state.user.role))return go("home");
 const d=await api("/admin/overview");setApp(`<div class="welcome"><div><h1>Admin boshqaruv paneli</h1><p class="muted">MAKTAB X platformasining barcha asosiy ko‘rsatkichlari.</p></div><button class="primary" onclick="productModal()">+ Mahsulot qo‘shish</button></div><div class="admin-kpis" style="margin:20px 0">${Object.entries(d.counts).map(([k,v])=>`<div class="mini"><small class="muted">${label(k)}</small><b>${money(v)}</b></div>`).join("")}</div><div class="grid2"><div class="card"><div class="section-title"><h2>Foydalanuvchilar</h2></div><table><thead><tr><th>Ism</th><th>Role</th><th>X Coin</th></tr></thead><tbody>${d.users.map(u=>`<tr><td>${esc(u.name)}</td><td><span class="badge">${u.role}</span></td><td>${money(u.balance)}</td></tr>`).join("")}</tbody></table></div><div class="card"><div class="section-title"><h2>So‘nggi xaridlar</h2></div>${d.purchases.slice(0,6).map(p=>`<div class="list-item"><div><b>${esc(p.user||"")}</b><div class="muted">${p.items?.length||0} mahsulot · ${p.total} X Coin</div></div><span class="badge gold">${p.status}</span></div>`).join("")||"<p class='muted'>Xaridlar yo‘q</p>"}</div></div>`);
}
function label(k){return ({students:"O‘quvchilar",teachers:"O‘qituvchilar",classes:"Sinflar",products:"Mahsulotlar",purchases:"Xaridlar",coinsEarned:"Topilgan Coin",coinsSpent:"Sarflangan Coin"})[k]||k}
function productModal(){document.body.insertAdjacentHTML("beforeend",`<div class="modal-back" id="modal"><div class="modal"><div class="modal-head"><h2>CoinShop mahsuloti</h2><button class="ghost" onclick="modal.remove()">✕</button></div><form id="pf"><div class="field"><label>Nomi</label><input id="pn" required></div><div class="field"><label>Tavsif</label><textarea id="pd"></textarea></div><div class="grid2"><div class="field"><label>X Coin narxi</label><input id="pp" type="number" required></div><div class="field"><label>Ombor</label><input id="pst" type="number" required></div></div><div class="field"><label>Kategoriya</label><input id="pc" value="Maktab anjomlari"></div><div class="field"><label>Rasm</label><input id="pi" type="file" accept="image/png,image/jpeg,image/webp"></div><button class="primary">Saqlash</button></form></div></div>`);pf.onsubmit=async e=>{e.preventDefault();const fd=new FormData();fd.append("name",pn.value);fd.append("description",pd.value);fd.append("price",pp.value);fd.append("stock",pst.value);fd.append("category",pc.value);if(pi.files[0])fd.append("image",pi.files[0]);try{await fetch("/api/admin/products",{method:"POST",body:fd,credentials:"include"}).then(async r=>{if(!r.ok)throw new Error((await r.json()).error)});modal.remove();render();toast("Mahsulot qo‘shildi")}catch(e){toast(e.message)}}}
function renderProfile(){setApp(`<div class="welcome"><div><h1>Profil</h1><p class="muted">Shaxsiy hisob ma’lumotlari.</p></div></div><div class="card" style="margin-top:20px;max-width:700px"><div class="rowflex"><div class="avatar" style="width:70px;height:70px;font-size:28px">${esc(state.user.name[0])}</div><div><h2>${esc(state.user.name)}</h2><p class="muted">${esc(state.user.email)}</p><span class="badge">${state.user.role}</span></div></div><hr style="border:0;border-top:1px solid var(--line);margin:22px 0"><p><b>Subscription:</b> PRO</p><p><b>X Coin:</b> ${money(state.user.balance)}</p><button class="danger" onclick="logout()">Chiqish</button></div>`)}

async function logout(){await api("/auth/logout",{method:"POST"});state.user=null;renderLogin()}
boot();
